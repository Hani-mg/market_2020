import { AutoTranslationDirective } from './auto-translation.directive';

describe('AutoTranslationDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoTranslationDirective();
    expect(directive).toBeTruthy();
  });
});
